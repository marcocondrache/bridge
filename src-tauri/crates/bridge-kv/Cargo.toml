[package]
name = "bridge-kv"
version.workspace = true
edition.workspace = true

[lib]
name = "bridge_kv"

[dependencies]
anyhow = { workspace = true }
sqlx = { workspace = true }

bridge-db = { path = "../bridge-db" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
