[package]
name = "bridge-history"
version.workspace = true
edition.workspace = true

[lib]
name = "bridge_history"

[dependencies]
bridge-http = { path = "../bridge-http" }
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
